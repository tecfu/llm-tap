# Port of the localhost engine to tap — the tap fronts http://127.0.0.1:$PORT.
# Override per invocation without editing this file:
#   PORT=8080 docker compose up -d
PORT=8000

# Client-facing tap port (agents point here).
TAP_PORT=8001
